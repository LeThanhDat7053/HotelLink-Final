import type { FC } from 'react';

/**
 * AboutPage - Now minimal since layout is handled by App.tsx
 * Content is managed centrally in AppLayout metadata
 */
const AboutPage: FC = () => {
  return null; // Layout and content managed by AppLayout
};

export default AboutPage;
