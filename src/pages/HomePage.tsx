import type { FC } from 'react';

/**
 * HomePage - Now minimal since layout is handled by App.tsx
 * VR360 and InfoBox content are managed centrally in AppLayout
 */
const HomePage: FC = () => {
  return null; // Layout and content managed by AppLayout
};

export default HomePage;
