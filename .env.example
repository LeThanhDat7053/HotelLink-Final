# ===========================================
# 🏨 HOTEL FRONTEND TEMPLATE - CONFIGURATION
# ===========================================
# Copy file này thành .env và điền giá trị thực
# 
# ⚠️ TẤT CẢ biến VITE_ đều BẮT BUỘC phải có giá trị
# ===========================================

# ============ 🔗 API Configuration ============
# Base URL cho API (không có dấu / ở cuối)
VITE_API_BASE_URL=https://travel.link360.vn/api/v1

# ============ 🔐 Authentication ============
# Liên hệ admin để lấy credentials
VITE_API_USERNAME=your_email@example.com
VITE_API_PASSWORD=your_password_here

# ============ 🏷️ Hotel Identity ============
# Tenant Code - mã định danh khách sạn (lấy từ backend admin)
VITE_TENANT_CODE=your_tenant_code

# Property ID - ID của property (lấy từ backend admin)
VITE_PROPERTY_ID=123

# Tenant ID - ID của tenant (lấy từ backend admin)
VITE_TENANT_ID=1

# ============ 🖼️ Media ============
# Logo Media ID - ID của logo trong hệ thống media
# Để trống nếu chưa có, app sẽ tự fetch từ API settings
VITE_LOGO_MEDIA_ID=

# ============ 🌐 Website URLs ============
# URL chính của website khách sạn (không có dấu / ở cuối)
VITE_SITE_BASE_URL=https://yourhotel.example.com

# Tên hiển thị của khách sạn (hiển thị trên tab browser)
VITE_APP_NAME=Your Hotel Name

# CDN URL cho VR360 content
VITE_VR360_CDN_URL=https://travel.link360.vn

# ===========================================
# 📋 HƯỚNG DẪN SETUP KHÁCH SẠN MỚI:
# ===========================================
# 
# Bước 1: Copy file cấu hình
#   cp .env.example .env
#
# Bước 2: Lấy thông tin từ Backend Admin
#   - Đăng nhập vào admin panel
#   - Lấy: PROPERTY_ID, TENANT_CODE, TENANT_ID
#   - Lấy: API username/password
#   - (Optional) Lấy: LOGO_MEDIA_ID
#
# Bước 3: Cập nhật .env
#   - Điền tất cả thông tin vào .env
#   - Cập nhật SITE_BASE_URL theo domain thực tế
#
# Bước 4: Cập nhật index.html
#   - Thay thế thông tin SEO (title, description, keywords)
#   - Xem SETUP_CHECKLIST.md để biết chi tiết
#
# Bước 5: Chạy Development
#   npm install --legacy-peer-deps
#   npm run dev
#
# Bước 6: Build Production
#   npm run build
#   npm run inject-seo  # Tự động lấy SEO từ API
#
# ===========================================

# ============ 🔒 Security Notes

⚠️ **IMPORTANT:**
- NEVER commit `.env` to git
- Add `.env*` to `.gitignore`
- Use different credentials for dev/staging/production
- Rotate passwords regularly
- Don't expose sensitive data in frontend code

## .gitignore

Make sure `.gitignore` includes:

```
# Environment variables
.env
.env.local
.env.development
.env.production
.env*.local
```
