# Environment Variables Template
# Copy file này thành .env và điền giá trị thực

# API Configuration
VITE_API_BASE_URL=https://travel.link360.vn/api/v1

# Authentication (Liên hệ admin để lấy credentials)
VITE_API_USERNAME=your_email@example.com
VITE_API_PASSWORD=your_password_here
VITE_TENANT_CODE=your_tenant_code

# Property Configuration
VITE_PROPERTY_ID=your_property_id
VITE_TENANT_ID=your_tenant_id

# Site Configuration (for SEO)
VITE_SITE_BASE_URL=https://yourhotel.com

# Optional
VITE_APP_NAME=Your Hotel Name
VITE_VR360_CDN_URL=https://travel.link360.vn
// Correct way to access env vars in Vite
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL;
const appName = import.meta.env.VITE_APP_NAME;

console.log('API URL:', apiBaseUrl);
```

## TypeScript Type Definitions

Thêm vào `src/vite-env.d.ts`:

```typescript
/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_API_BASE_URL: string;
  readonly VITE_API_USERNAME?: string;
  readonly VITE_API_PASSWORD?: string;
  readonly VITE_TENANT_CODE?: string;
  readonly VITE_APP_NAME?: string;
  readonly VITE_VR360_CDN_URL?: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}
```

## Security Notes

⚠️ **IMPORTANT:**
- NEVER commit `.env` to git
- Add `.env*` to `.gitignore`
- Use different credentials for dev/staging/production
- Rotate passwords regularly
- Don't expose sensitive data in frontend code

## .gitignore

Make sure `.gitignore` includes:

```
# Environment variables
.env
.env.local
.env.development
.env.production
.env*.local
```
